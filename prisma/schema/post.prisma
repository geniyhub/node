model Post {
  id          Int       @id @default(autoincrement())
  name        String
  author      String
  description String
  date        DateTime
  userId      Int?
  user        User?      @relation(fields: [userId], references: [id])
  comment     Comment[]
}